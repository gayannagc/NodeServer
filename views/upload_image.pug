extends layout

block content
    form(method="POST", action='/image_upload/'+file.filename , enctype='multipart/form-data')
        input.custom-file-input.form-control(for="fileNew",type='file', name='fileNew')
        label.custom-file-lael(for='fileNew') Choose File
        br
        input.btn.btn-primary.btn-block.my-4(type='submit', value='Submit')