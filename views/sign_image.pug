extends layout

block content
            .container
                div.row
                    div.col-md-6.m-auto
                        h1.text-center.display-4.my-4 Add Sign Image
                        form(action='#', method='POST', enctype='multipart/form-data')
                            input.custom-file-input.form-control(for="file",type='file', name='file')
                            label.custom-file-lael(for='file') Choose File
                            br
                            input.btn.btn-primary.btn-block.my-4(type='submit', value='Submit')
                     
                        
                                
                        